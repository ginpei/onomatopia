<%= form_for(@onomatopoeia) do |f| %>
  <% if @onomatopoeia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@onomatopoeia.errors.count, "error") %> prohibited this onomatopoeia from being saved:</h2>

      <ul>
      <% @onomatopoeia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
