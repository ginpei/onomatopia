<%= form_for(@onomatope) do |f| %>
  <% if @onomatope.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@onomatope.errors.count, "error") %> prohibited this onomatope from being saved:</h2>

      <ul>
      <% @onomatope.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
